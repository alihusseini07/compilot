apiVersion: v1
kind: ConfigMap
metadata:
  name: compilot-config
  namespace: default
data:
  PORT: "8000"
  LOG_LEVEL: "info"
  LOOKBACK_DAYS: "30"
  OLLAMA_MODEL: "gemma4:26b"
  CORS_ORIGINS: "http://155.138.128.101"
  # OLLAMA_HOST is in the compilot-secrets Secret (contains the VM's internal IP)
