apiVersion: v1
kind: ConfigMap
metadata:
  name: compilot-config
  namespace: default
data:
  PORT: "8000"
  LOG_LEVEL: "info"
  LOOKBACK_DAYS: "30"
  INFERENCE_MODEL: "nvidia/DeepSeek-V3.2-NVFP4"
  CORS_ORIGINS: "http://155.138.128.101"
  # INFERENCE_API_KEY and INFERENCE_BASE_URL are in compilot-secrets
