apiVersion: batch/v1
kind: CronJob
metadata:
  name: pricing-scraper
  namespace: default
spec:
  schedule: "0 */6 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: pricing-scraper
              image: ahusseini07/compilot-backend:latest
              command:
                - python
                - -c
                - |
                  import asyncio, os
                  from agents.pricing_agent import PricingAgent
                  companies = os.environ.get("TRACKED_COMPANIES", "linear").split(",")
                  async def main():
                      agent = PricingAgent()
                      for c in companies:
                          await agent.run(c.strip())
                  asyncio.run(main())
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: compilot-secrets
                      key: DATABASE_URL
                - name: TRACKED_COMPANIES
                  value: "linear,notion,figma"
              resources:
                requests:
                  cpu: "100m"
                  memory: "128Mi"
                limits:
                  cpu: "300m"
                  memory: "256Mi"
