apiVersion: apps/v1
kind: Deployment
metadata:
  name: compilot-backend
  namespace: default
  labels:
    app: compilot-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: compilot-backend
  template:
    metadata:
      labels:
        app: compilot-backend
    spec:
      containers:
        - name: backend
          image: ahusseini07/compilot-backend:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: compilot-config
          env:
            - name: OLLAMA_HOST
              valueFrom:
                secretKeyRef:
                  name: compilot-secrets
                  key: OLLAMA_HOST
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compilot-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compilot-secrets
                  key: REDIS_URL
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: compilot-secrets
                  key: GITHUB_TOKEN
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: compilot-backend
  namespace: default
spec:
  selector:
    app: compilot-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
